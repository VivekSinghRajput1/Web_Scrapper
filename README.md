A Python-based Threat Intelligence and Web Analysis tool that performs automated URL scanning to extract security insights from websites.
It analyzes HTTP responses, detects security headers, identifies external resources, resolves domain IPs, and extracts Indicators of Compromise (IOCs) such as IPs, domains, emails, and CVE references.
The tool also detects basic anti-bot protections and generates structured JSON reports for security analysis.
